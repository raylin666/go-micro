// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//+build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
	"upload_service/internal/biz"
	"upload_service/internal/conf"
	"upload_service/internal/data"
	"upload_service/internal/server"
	"upload_service/internal/service"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(confServer *conf.Server, confData *conf.Data, logger log.Logger) (*kratos.App, func(), error) {
	dataData, cleanup, err := data.NewData(confData, logger)
	if err != nil {
		return nil, nil, err
	}
	uploadRepo := data.NewUploadRepo(dataData, logger)
	uploadUsecase := biz.NewUploadUsecase(uploadRepo, logger)
	uploadService := service.NewUploadService(uploadUsecase, logger)
	httpServer := server.NewHTTPServer(confServer, uploadService, logger)
	grpcServer := server.NewGRPCServer(confServer, uploadService, logger)
	app := newApp(logger, httpServer, grpcServer)
	return app, func() {
		cleanup()
	}, nil
}
